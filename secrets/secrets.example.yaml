# Copy shape only — real file is secrets/secrets.yaml (encrypted).
# Edit encrypted secrets:  sops secrets/secrets.yaml
notify-webhook: "https://hooks.slack.com/services/..."
agent-env: |
  export ANTHROPIC_API_KEY="sk-..."
  export OPENAI_API_KEY="sk-..."
